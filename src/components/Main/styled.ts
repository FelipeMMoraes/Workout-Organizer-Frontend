import styled from 'styled-components'

const Container = styled.div`
  grid-area: main;

  padding: 36px;
  display: grid;
  grid-template-columns: 750px 1fr;
  gap: 30px;
`

const LeftMain = styled.div``
const RightMain = styled.div``

export { Container, LeftMain, RightMain }
